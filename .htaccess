<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Set the base for subdirectory
    RewriteBase /UServe/
    
    # Redirect all requests to public folder (visible in browser)
    RewriteCond %{REQUEST_URI} !^/UServe/public/
    RewriteRule ^(.*)$ /UServe/public/$1 [R=301,L]
</IfModule>